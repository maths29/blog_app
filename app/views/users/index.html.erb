<% if @users.any? %>
  <%= link_to "Add a new post", new_user_post_path(user_id: @user.id), class: "btn btn-primary"%>

  <% @users.each do |user| %>
    <%= render "shared/user", user: user, class: "user"  %>
  <% end %>
<% else %>
  <h1 class="text-center m-auto">No users yet</h1>
<% end %>
